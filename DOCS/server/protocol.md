`packages/server/src/protocol.ts` — minimal re-export.

Summary:
- `export * from "@privatefrp/shared"` — re-exports shared protocol constants and types (message types, `TunnelConfig`, frame encoder/decoder, etc.).
