{
  "name": "@privatefrp/agent",
  "version": "1.0.0",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "build": "bun build src/index.ts --compile --outfile ../../dist/agent"
  },
  "dependencies": {
    "@privatefrp/shared": "workspace:*"
  }
}
