{
  "name": "@privatefrp/agent",
  "version": "1.0.0",
  "scripts": {
    "dev": "bun --env-file=../../agent.env run --watch src/index.ts",
    "start": "bun --env-file=../../agent.env run src/index.ts",
    "build": "bun build src/index.ts --compile --outfile ../../dist/agent"
  },
  "dependencies": {
    "@privatefrp/shared": "workspace:*"
  }
}
